# Django
SECRET_KEY=django-geheimer-schluessel-hier-aendern
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173

# Datenbank
DB_NAME=mitarbeiterapp
DB_USER=mitarbeiter
DB_PASSWORD=sicher123
DB_HOST=db
DB_PORT=5432

# Admin (1:1 aus altem backend/.env kopieren!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2y$10$HIER_DEN_HASH_AUS_PHP_ENV_EINTRAGEN
ADMIN_ROLE=customer_admin
CUSTOMER_KEY=default

# SMTP (1:1 aus altem backend/.env kopieren!)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=mail@example.com
SMTP_PASSWORD=passwort
SMTP_ENCRYPTION=tls
FROM_EMAIL=mail@example.com
FROM_NAME=Mitarbeiter Pro
RECIPIENT_EMAIL=chef@example.com

# Verschlüsselung (GLEICHER KEY wie in PHP backend/.env!)
ENCRYPTION_KEY=mitarbeiterapp_secret_key_change_in_production_2024

# JWT
JWT_SECRET=jwt-geheimer-schluessel-hier-aendern
JWT_EXPIRE_HOURS=8
JWT_COOKIE_NAME=jwt
JWT_COOKIE_SECURE=False

# Employee-Device-Cookies fuer Mitarbeiter-Flow
# Bei getrennter Frontend-/API-Domain muss SameSite=None und Secure=True sein.
EMPLOYEE_DEVICE_COOKIE_SECURE=True
EMPLOYEE_DEVICE_COOKIE_SAMESITE=None

# Spam-Schutz
PDF_API_SECRET=hier-einen-langen-zufaelligen-string-eintragen
PDF_RATE_LIMIT=10
