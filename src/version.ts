// src/version.ts
// Automatisch generiert: Stellt die Version aus package.json bereit

// Vite/ESM-kompatibler Import der Version aus package.json
import pkg from '../package.json';
export const APP_VERSION = pkg.version;
